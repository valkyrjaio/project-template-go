# NOTE: references a Go reusable workflow (_update-go-dependencies.yml) that is
# not yet authored in valkyrjaio/.github. This workflow is scheduled/manual and
# will only run once that reusable workflow lands.
name: Update Dependencies

on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  update-dependencies:
    uses: valkyrjaio/.github/.github/workflows/_update-go-dependencies.yml@bcbf9696bd9a7fd71abcf4d00d3420cc949c98e1
    secrets: inherit
    with:
      dependencies: |
        [
          {"name": "Root Module",   "directory": "."},
          {"name": "golangci-lint", "directory": ".github/ci/lint"}
        ]
